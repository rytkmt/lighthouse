# lighthouse

自分用の Neovim カラースキーム。

夜の海に灯る灯台をイメージした配色で、暗い背景に柔らかな光が浮かぶような色合いを目指している。

## Install

lazy.nvim:

```lua
{ "rytkmt/lighthouse", config = function() vim.cmd("colorscheme lighthouse") end }
```
